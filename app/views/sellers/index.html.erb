<h1 > Make this view accessible only for admin staff - it should not be accessible via any normal route  - dangerous!</h1>
<h1>Listing sellers</h1>

<table>
  <tr>
    <th>Number sold items</th>
  </tr>

<% for seller in @sellers %>
  <tr>
    <td><%=h seller.number_sold_items %></td>
    <td><%= link_to 'Show', seller %></td>
    <td><%= link_to 'Edit', edit_seller_path(seller) %></td>
    <td><%= link_to 'Destroy', seller, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New seller', new_seller_path %>
